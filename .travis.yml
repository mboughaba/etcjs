language: node_js
node_js:
    - "8.9.0"
    - "9.0.0"
before_install:
    - "npm install -g npm@latest"
script:
    - "npm run test-travis"
after_script:
    - "npm install coveralls@2.11.x && cat coverage/lcov.info | coveralls"
