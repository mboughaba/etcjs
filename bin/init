#!/usr/bin/env node

var config = require(__dirname + '/../lib/config');
console.dir(process.argv);
console.log(process.argv[2]);
config({dir: process.argv[2]}, function() {
    console.info('configuration has been initialized');
});
